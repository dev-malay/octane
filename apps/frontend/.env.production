# Production env for Vercel builds (public URLs only — these ship in the browser bundle by design).
# NOTE: kept in git intentionally so Vercel prod builds work without dashboard env vars.
# Do NOT put secrets here. Server secrets (JWT, DATABASE_URL) live on Render only.
VITE_API_URL=https://octane-5yti.onrender.com/api/v1
VITE_WS_URL=wss://ws-ixog.onrender.com
